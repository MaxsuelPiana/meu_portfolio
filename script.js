// Futuramente: pode-se adicionar scroll automático nos rovers, ou interações no cubo
console.log("Portfólio de Maxsuel Piana carregado!");

window.addEventListener('scroll', function() {
    const contatosSection = document.getElementById('contatos');
    const rect = contatosSection.getBoundingClientRect();
});
    
